package org.drools.workwithstring

rule "UpperCase string"
    when
        $s : String ()
    then
        WorkWithString wws = new WorkWithString($s);
        System.out.println(wws.toUpperCase());
end